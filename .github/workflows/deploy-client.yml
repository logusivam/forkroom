name: Deploy Client

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Deploy static build to Vercel
      run: echo "Deploys client workspace to Vercel"

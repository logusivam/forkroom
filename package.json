{
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^9.1.0",
    "lint-staged": "^15.2.0"
  },
  "lint-staged": {
    "client/src/**/*.{ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "server/src/**/*.{js}": [
      "eslint --fix",
      "prettier --write"
    ]
  }
}
